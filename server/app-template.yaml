runtime: python312

entrypoint: uvicorn main:app 

env_variables:
  SOME_ENV_VAR: "value"  # Add any environment variables here, if needed

handlers:
  - url: /static
    static_dir: static  # Serve static files, if any
  - url: /.*  # Handle all other requests with the FastAPI app
    script: auto
